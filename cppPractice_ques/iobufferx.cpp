#include<iostream>
#include<bits/stdc++.h>

using namespace std;

int main(){
    int a;
    char str[80];
    cin>>a;
    cin.sync();
    // cin>>ws;
    // cin.ignore();
    cin.getline(str,80);
    cout<<a<<endl<<str<<endl;
    return 0;
}